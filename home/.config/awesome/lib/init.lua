return {
	dbus_proxy = require("lib.dbus_proxy"),
	inspect = require("lib.inspect"),
	json = require("lib.json"),
	string = require("lib.string"),
	table = require("lib.table"),
	file = require("lib.file"),
	shape = require("lib.shape")
}
