return {
	audio = require("service.audio"),
	bluetooth = require("service.bluetooth"),
	network = require("service.network"),
	screenshot = require("service.screenshot"),
	weather = require("service.weather")
}
